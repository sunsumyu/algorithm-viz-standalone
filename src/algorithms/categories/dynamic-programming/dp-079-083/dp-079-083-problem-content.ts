/**
 * 左神算法通关课 Class 079 ~ 083 进阶动态规划专题 题目与教学大纲
 */

export const DP_079_083_PROBLEMS = {
  digitDp079: `
### Class 079: 数位 DP 基础模型 (Digit DP)

#### 核心知识点与大纲
1. **数位拆分与记忆化递归**：
   - 将上界数字 $N$ 转化为高位到低位的数位数组 $digits[len-1 \\dots 0]$。
   - 定义状态：\`f(idx, isLimit, isNum, ...)\`：
     - \`idx\`: 当前正在决策第几位（从高位到低位）。
     - \`isLimit\`: 前面所填数字是否全部贴着上界。若为 true，当前位最高只能填到 $digits[idx]$；若为 false，则可自由填 $0 \\dots 9$。
     - \`isNum\`: 前面是否已经填了有效数字（处理前导零问题）。
2. **记忆化约束**：
   - 只有在 \`!isLimit && isNum\`（即无上限限制且无前导零限制的自由状态）下，子问题的结果才具有通用性，可以安全写入记忆化缓存 \`dp[idx][...]\`。
3. **经典应用**：
   - LeetCode 233 / 357 / 600 / 902：统计满足特定数位约束（如数字 1 出现总次数、无连续 1、各数位互不相同）的数字个数。

#### 复杂度分析
- **时间复杂度**：$O(\\text{Len} \\times \\Sigma \\times \\text{States})$，其中 $\\text{Len} = O(\\log_{10} N)$，比暴力枚举 $O(N)$ 产生质的飞跃。
- **空间复杂度**：$O(\\text{Len} \\times \\text{States})$。
`,

  rerootingDp080: `
### Class 080: 换根 DP 专题 (Rerooting Tree DP)

#### 核心知识点与大纲
1. **两次 DFS 思想 (Up-Down Tree DP)**：
   - 传统树形 DP 只能以固定根（如节点 0）为基准求解单根答案，若要求出**以树上每个节点为根时的全局最优解**，暴力重复遍历需要 $O(N^2)$。
   - 换根 DP 采用“两遍 DFS”范式，以 $O(N)$ 求解全树所有节点作为根的答案。
2. **DFS 1（自底向上）**：
   - 选定 0 号点为临时根，计算每个子树的规模 $size[u]$，以及以 0 为根时子树内的距离和 $ans[0]$。
3. **DFS 2（自顶向下 / 换根转移）**：
   - 当根从父节点 $u$ 转移到其相邻子节点 $v$ 时：
     - 子节点 $v$ 的子树内所有节点到新根的距离全部 $-1$（共 $size[v]$ 个点）。
     - 其余全树节点（$N - size[v]$ 个点）到新根的距离全部 $+1$。
     - 换根状态转移方程：
       $$ans[v] = ans[u] - size[v] + (N - size[v]) = ans[u] + N - 2 \\times size[v]$$
4. **经典题目**：
   - LeetCode 834: 树中所有节点对的距离之和 (Sum of Distances in Tree)。

#### 复杂度分析
- **时间复杂度**：$O(N)$，两次 DFS 遍历整棵树。
- **空间复杂度**：$O(N)$，树邻接表与状态数组。
`,

  expectedValueDp081: `
### Class 081: 期望 DP 与马尔可夫决策过程 (Expected Value DP)

#### 核心知识点与大纲
1. **概率与期望的线性性质**：
   - 数学期望的加法定理：$E(X + Y) = E(X) + E(Y)$，不受变量是否独立限制。
   - 概率逆推与顺推模型：
     - 求从当前状态出发到达目标状态的**期望步数/代价**：通常采用**倒序逆推**：
       $$E[u] = 1 + \\sum_{v} P(u \\to v) \\times E[v]$$
     - 求从起点出发到达各个状态的**概率**：通常采用**正序顺推**。
2. **马尔可夫决策与全概率展开**：
   - 经典棋盘马走日概率模型（LeetCode 688）：
     - $dp[k][r][c]$ 表示从位置 $(r, c)$ 出发再走 $k$ 步仍在棋盘内的概率。
     - 转移方程：$dp[k][r][c] = \\frac{1}{8} \\sum_{(nr, nc) \\in \\text{valid}} dp[k-1][nr][nc]$。
3. **边界与吸收态**：
   - 终点态（目标状态）期望代价为 0；无效/越界态概率为 0。

#### 复杂度分析
- **时间复杂度**：$O(K \\times N \\times M)$，其中 $K$ 为步数。
- **空间复杂度**：$O(N \\times M)$ 滚动数组优化。
`,

  slopeOptDp082: `
### Class 082: 斜率优化 DP 与单调队列凸包 (Slope Optimization DP)

#### 核心知识点与大纲
1. **二次项交叉项优化本质**：
   - 经典 1D DP 转移方程形如：$dp[i] = \\min_{0 \\le j < i} \\{ dp[j] + (s[i] - s[j])^2 \\}$。
   - 展开后存在含有下标 $i$ 与 $j$ 相互耦合的**交叉项** $-2 s[i] s[j]$，无法直接用线段树或单调队列直接维护单变量。
2. **几何直线性质转换**：
   - 将转移式改写为直线点斜式方程：$Y = K \\cdot X + B$：
     - $Y = dp[j] + s[j]^2$
     - $X = s[j]$
     - $K = 2 s[i]$
     - 截距 $B = dp[i] - s[i]^2$
   - 要让 $dp[i]$ 最小，即要求直线截距 $B$ 最小。在平面上由点 $(X_j, Y_j)$ 构成的点集中，寻找一条斜率为 $K$ 的切线切过点集，所能达到的最小截距。
3. **单调队列凸包维护**：
   - 最优决策点必然落在点集的**下凸壳（Lower Convex Hull）**上。
   - 当查询斜率 $K$ 和横坐标 $X$ 均单调递增时，可用**单调双端队列**动态维护下凸壳相邻线段的斜率递增性。
   - 队头根据当前斜率 $K$ 弹出不优决策点；队尾在新点加入时若破坏下凸性则弹出队尾点。
   - 复杂度由 $O(N^2)$ 降维至极致的严格 $O(N)$！

#### 复杂度分析
- **时间复杂度**：$O(N)$ 均摊时间，每个点进出单调队列各 1 次。
- **空间复杂度**：$O(N)$ 单调队列与前缀和。
`,

  knuthQuadrangle083: `
### Class 083: 四边形不等式与决策单调性优化 (Knuth's Quadrangle Inequality)

#### 核心知识点与大纲
1. **区间 DP 经典瓶颈**：
   - 石子合并、最优二叉搜索树、邮局选址等经典区间 DP 转移形如：
     $$dp[i][j] = \\min_{i \\le k < j} \\{ dp[i][k] + dp[k+1][j] \\} + w(i, j)$$
   - 朴素枚举分割点 $k$ 导致总时间复杂度高达 $O(N^3)$。
2. **四边形不等式与包含单调性**：
   - 若代价函数 $w(i, j)$ 满足四边形不等式：对任意 $a \\le b \\le c \\le d$，有
     $$w(a, d) + w(b, c) \\ge w(a, c) + w(b, d)$$
   - 则 $dp[i][j]$ 同样满足四边形不等式，且其最优决策点 $opt[i][j]$ 满足**决策单调性**：
     $$opt[i][j-1] \\le opt[i][j] \\le opt[i+1][j]$$
3. **Knuth 决策区间剪枝**：
   - 枚举 $k$ 的搜索范围被严格限制在 $[opt[i][j-1], opt[i+1][j]]$ 之内。
   - 借由二维级数裂项相消：
     $$\\sum_{i} (opt[i+1][j] - opt[i][j-1]) = O(N)$$
   - 整体区间 DP 复杂度由 $O(N^3)$ 降至 $O(N^2)$！

#### 复杂度分析
- **时间复杂度**：由朴素 $O(N^3)$ 优化至严格 $O(N^2)$。
- **空间复杂度**：$O(N^2)$ 记录 DP 与决策表。
`,
};
