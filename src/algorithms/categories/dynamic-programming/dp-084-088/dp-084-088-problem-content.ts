/**
 * 左神算法通关课 Class 084 ~ 088 进阶动态规划专题（第二弹）题目与教学大纲
 */

export const DP_084_088_PROBLEMS = {
  countingDp084: `
### Class 084: 计数 DP 与容斥原理 (Counting DP & Inclusion-Exclusion)

#### 核心知识点与大纲
1. **加法原理与乘法原理在 DP 中的体现**：
   - 计数 DP 要求满足“不重不漏”（互斥划分与全集覆盖）。
   - 状态定义：$dp[i]$ 表示规模为 $i$ 且满足特定约束的方案数。
2. **经典错排递推公式**：
   - 考虑第 $n$ 个元素放错位置（假设放入位置 $k$，共 $n-1$ 种选法）：
     - 若第 $k$ 个元素正好放入位置 $n$：剩下 $n-2$ 个元素构成错排，方案数为 $D(n-2)$。
     - 若第 $k$ 个元素不放入位置 $n$：相当于将位置 $n$ 视作 $k$ 的禁放位置，剩余 $n-1$ 个元素构成错排，方案数为 $D(n-1)$。
     - 递推方程：
       $$D(n) = (n - 1) \\times [D(n - 1) + D(n - 2)], \\quad D(1) = 0, D(2) = 1$$
3. **容斥原理展开**：
   - 全排列减去至少有 1 个元素在原位的方案数：
     $$D(n) = n! \\sum_{i=0}^{n} \\frac{(-1)^i}{i!}$$

#### 复杂度分析
- **时间复杂度**：$O(N)$ 线性递推。
- **空间复杂度**：$O(1)$ 滚动变量。
`,

  gameProbability085: `
### Class 085: 博弈概率 DP 与倒推期望状态 (Game Probability DP)

#### 核心知识点与大纲
1. **零和博弈与极大极小定理 (Minimax Theorem)**：
   - 双方均以最优策略行事，先手希望最大化自己的净收益，后手同样希望最大化其收益（即最小化先手的净收益）。
2. **区间博弈 DP 状态设计**：
   - $dp[i][j]$ 表示当前面临剩余石子堆区间 $[i, j]$ 时，当前行动方相比于对手能获得的最大相对净得分（先手得分 - 后手得分）。
3. **状态转移方程**：
   - 当前行动方有两种决策：拿走最左侧 $piles[i]$，或拿走最右侧 $piles[j]$。
   - 对手在下一轮获得的最大净得分为 $dp[i+1][j]$ 或 $dp[i][j-1]$。
   - 故本方能获得的最大净得分：
     $$dp[i][j] = \\max(piles[i] - dp[i+1][j], \\; piles[j] - dp[i][j-1])$$
   - 最终若 $dp[0][n-1] \\ge 0$，则先手必胜。

#### 复杂度分析
- **时间复杂度**：$O(N^2)$，区间长度由 $1 \\dots N$ 递增。
- **空间复杂度**：$O(N)$ 滚动数组优化。
`,

  sosDp086: `
### Class 086: 高阶状压 DP 与 SOS DP / 子集和高维前缀和 (Sum Over Subsets)

#### 核心知识点与大纲
1. **子集和问题痛点**：
   - 给定布尔/权值数组 $A$，对于每个掩码 mask，求其所有子集 $sub \\subseteq mask$ 的权值和：
     $$F[mask] = \\sum_{sub \\subseteq mask} A[sub]$$
   - 暴力枚举子集 $\\sum \\binom{N}{k} 2^k = 3^N$。当 $N=20$ 时，$3^{20} \\approx 3.5 \\times 10^9$ 超时！
2. **SOS DP（高维前缀和 / 快速莫比乌斯变换）**：
   - 将二进制状态看作 $N$ 维超立方体，每一维只有 0 和 1 两个坐标。
   - 定义 $dp[i][mask]$ 表示只考虑前 $i$ 位作为自由位变化、其余更高位完全匹配 mask 时的子集和：
     - 若 mask 的第 $i$ 位为 0：$dp[i][mask] = dp[i-1][mask]$。
     - 若 mask 的第 $i$ 位为 1：$dp[i][mask] = dp[i-1][mask] + dp[i-1][mask \\oplus 2^i]$（包含了第 $i$ 位为 0 的所有子集）。
3. **复杂度飞跃**：
   - 维度循环 $i$ 从 $0 \\dots N-1$，掩码循环 $mask$ 从 $0 \\dots 2^N-1$。
   - 复杂度由 $O(3^N)$ 降至惊人的 $O(N \\cdot 2^N)$！$20 \\times 2^{20} \\approx 2 \\times 10^7$，常数极小毫秒级运行。

#### 复杂度分析
- **时间复杂度**：严格 $O(N \\cdot 2^N)$。
- **空间复杂度**：$O(2^N)$ 原地就地累加。
`,

  circularInterval087: `
### Class 087: 环形区间 DP 与破环成链 (Circular Interval DP)

#### 核心知识点与大纲
1. **环形问题的破环成链倍长技巧**：
   - 环状结构上任何连续长度为 $N$ 的切片都可能成为最优解起点。
   - 将长度为 $N$ 的环形数组**复制倍长拼接**成长度为 $2N$ 的线性数组：
     $$arr[i + N] = arr[i] \\quad (0 \\le i < N)$$
   - 这样环上任意起点、顺时针长度为 $N$ 的连续子段，都一一映射为倍长数组中区间 $[i, i + N - 1]$ 的线性区间 DP！
2. **能量项链 / 环形石子合并状态转移**：
   - 定义 $dp[i][j]$ 为合并区间 $[i, j]$ 所获得的最大/最小得分。
   - 区间 DP 经典划分：
     $$dp[i][j] = \\max_{i \\le k < j} \\{ dp[i][k] + dp[k+1][j] + \\text{cost}(i, k, j) \\}$$
3. **全局最优解获取**：
   - 遍历所有可能的环切断起点：$ans = \\max_{0 \\le i < N} dp[i][i + N - 1]$。

#### 复杂度分析
- **时间复杂度**：$O(N^3)$（配合四边形不等式可降至 $O(N^2)$）。
- **空间复杂度**：$O(N^2)$。
`,

  treeKnapsack088: `
### Class 088: 树上背包 DP 与泛化物品优化 (Tree Knapsack DP)

#### 核心知识点与大纲
1. **有树形依赖的背包问题（选课问题）**：
   - 物品之间存在树状拓扑依赖关系：选子节点课程前，必须先选修其所有祖先节点课程。
   - 建立虚拟超级根节点 0，将森林转换为以 0 为根的一棵大树。
2. **状态定义与分组背包**：
   - $dp[u][j]$ 表示在以 $u$ 为根的子树中，选择修读了 $j$ 门课程（含节点 $u$ 自身）所能获得的最大学分。
3. **上下界双重剪枝与子树合并优化**：
   - 暴力背包遍历复杂度可能退化至 $O(N \\cdot M^2)$。
   - 引入子树规模上界剪枝：合并子节点 $v$ 时，$j$ 从 $\\min(M, size[u])$ 倒序循环至 1，$k$ 从 $0 \\dots \\min(j - 1, size[v])$。
   - 转移方程：
     $$dp[u][j] = \\max_{0 \\le k < j} \\{ dp[u][j - k] + dp[v][k] \\}$$
   - 数学上严格等价于两两节点对在 LCA 处合并恰好一次，整体时间复杂度降至紧致的 $O(N \\cdot M)$！

#### 复杂度分析
- **时间复杂度**：树上点对相遇复杂度 $O(N \\cdot M)$。
- **空间复杂度**：$O(N \\cdot M)$。
`,
};
