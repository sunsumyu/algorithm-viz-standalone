/**
 * 左神算法通关课 Class 058 洪水填充高频题与连通块扩展 题目与教学大纲
 */

export const SEARCH_058_PROBLEMS = {
  makingLargeIsland058: `
### Class 058: 洪水填充高频扩展与最大人工岛 (Making A Large Island)

#### 核心知识点与大纲
1. **两次遍历与连通块染色标号**：
   - 传统网格连通块只做单次洪水填充（FloodFill）统计面积。
   - 最大人工岛（LeetCode 827）要求：允许将**恰好一个 0 翻转为 1**，求能形成的最大连通岛屿面积。
   - 暴力对每个 0 运行一次 DFS 会导致 $O(N^4)$ 超时。
2. **第一遍 DFS：岛屿染色与面积登记**：
   - 遍历网格，为每个不同的连通岛屿分配独立 ID（例如从 2 开始，避免与 0 和 1 冲突）。
   - 将连通块内部所有格子值改写为其岛屿 ID，并用哈希表/数组记录：\`area[id] = 连通面积\`。
3. **第二遍遍历：桥接枚举与四邻合并**：
   - 枚举每一个数字为 0 的海洋格子 $(r, c)$：
   - 探查其上下左右 4 个邻居格子，使用哈希集合 \`Set<Integer>\` 记录相邻的所有**不同岛屿 ID**（防止同一岛屿被多次累加）。
   - 翻转当前 0 后的合并面积为：
     $$\\text{CombinedArea} = 1 + \\sum_{id \\in \\text{neighborIds}} \\text{area}[id]$$
   - 更新全局最大面积 $\\maxArea$。
4. **特判全 1 网格**：
   - 若原网格全是 1（没有 0），则最大人工岛面积即为 $N \\times N$。

#### 复杂度分析
- **时间复杂度**：严格 $O(N^2)$，第一遍 DFS 染色访问每个格子常数次；第二遍枚举每个 0 检查 4 个邻居，常数极小。
- **空间复杂度**：$O(N^2)$ 递归调用栈与面积哈希表。
`,
};
